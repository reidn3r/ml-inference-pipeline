apiVersion: v1
kind: PersistentVolume
metadata:
  name: pgsql-pv
  labels:
    app: pgsql-volume
    tier: storage
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/pgsql/data"
  capacity:
    storage: 2Gi
